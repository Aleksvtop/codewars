Написать функцию, которая переписывает xor на язык js

Мое решение:
const xor = (a, b) => !(a===b)

Другие решения:
function xor(a, b) {
  if ((a === true) && (b !== true)){
  return true
  } else if ((a !== true) && (b === true)){
  return true
  } else {
  return false
  }
}

xor = (a, b) => { return a==b ? false : true }
