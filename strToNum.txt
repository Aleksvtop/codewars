Написать функцию, которая преобразует строку в число (строка валидная)
Мое решение

const stringToNumber = str => Number(str);

Другие решения

var stringToNumber = function(str){
  return parseInt(str);
}

var stringToNumber = function(str){
  return +str;
}